spring:
  application:
    name: PIZZA-SERVICE
  config:
    import: optional:configserver:http://localhost:8888/config
  cloud:
    config:
      name: pizza-service
# Import the properties from the ConfigService.